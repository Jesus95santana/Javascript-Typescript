/**
 *  Because TypeScript knows what types our objects are,
 *  it also knows what shapes our objects adhere to. An object’s
 *  shape describes, among other things, what properties and methods
 *  it does or doesn’t contain.
 */

let firstName = "muriel!";

console.log(firstName.toUpperCase());

console.log(firstName.length);
