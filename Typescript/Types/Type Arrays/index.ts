/**
 * The TypeScript type annotation for array
 * types is fairly straightforward: we put [] after the element type.
 * In this code, names is an Array that can only contain strings:
 */

let names: string[] = ["Danny", "Samantha"];
