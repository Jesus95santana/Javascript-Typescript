/**
 * So far we’ve looked at string[] arrays,
 * but we could also have arrays that only contain numbers
 * (number[]) or booleans (boolean[]). In fact, we can make
 * arrays of any type whatsoever. We can also declare
 * multidimensional arrays: arrays of arrays (of some type).
 */

let arr: string[][] = [
	["str1", "str2"],
	["more", "strings"],
];
